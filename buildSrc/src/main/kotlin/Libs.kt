object Libs {
    object Jetbrains {
        const val annotations = "org.jetbrains:annotations:${Versions.Jetbrains.annotations}"
    }

    object SpringBoot {
        const val mybatis =
            "org.mybatis.spring.boot:mybatis-spring-boot-starter:${Versions.SpringBoot.mybatis}"
    }

    object Mybatis {
        const val mybatisSpring = "org.mybatis:mybatis-spring:${Versions.Mybatis.mybatisSpring}"
    }
}